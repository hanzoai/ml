// Relocated to hanzo-cli/tests/qmmq_numeric.rs — that crate links the HIP runtime via its build.rs
// and the engine workspace resolves cleanly (the ml workspace has wasm-only members that break
// native cargo resolution). Kept here as an empty placeholder.
